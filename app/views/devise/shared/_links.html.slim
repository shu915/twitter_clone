div.p-devise__links
  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = button_to "#{OmniAuth::Utils.camelize(provider)}でログイン", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: "c-button c-button__#{OmniAuth::Utils.camelize(provider)}"
      br

  - if controller_name != 'sessions'
    = link_to "ログインする", new_session_path(resource_name), class: 'p-devise__link'
    br
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "登録する", new_registration_path(resource_name), class: 'p-devise__link'
    br
  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "パスワードを忘れました。", new_password_path(resource_name), class: 'p-devise__link'
    br
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "確認メールが届かなかった。", new_confirmation_path(resource_name), class: 'p-devise__link'
    br
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = link_to "アカウントのロックを解除したい。", new_unlock_path(resource_name), class: 'p-devise__link'
    br
  
